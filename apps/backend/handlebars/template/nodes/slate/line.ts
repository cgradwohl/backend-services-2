import { Block } from "slate";

const getHandlebarsFromSlateLineBlock = (
  node: Block,
  children: string
): string => {
  // TODO:
  return children;
};

export default getHandlebarsFromSlateLineBlock;
